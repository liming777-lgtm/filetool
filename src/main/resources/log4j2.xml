<?xml version="1.0" encoding="UTF-8"?>
<!--monitorInterval 默认多长时间自动获取配置-->
<Configuration status="warn" monitorInterval="5">
    <properties>
        <property name="LOG_HOME">logs</property>
        <property name="FILE_NAME">tool</property>
    </properties>
    <Appenders>
        <!--在控制台中显示-->
        <Console name="Console" target="SYSTEM_OUT">
            <!--显示的格式-->
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] [%-5level] %c{36}:%L --- %m%n"/>
        </Console>
        <!--在文件中储存 循环储存一天一个  fileName 文件名字.filePattern 文件名重复后生成的名字-->
        <RollingRandomAccessFile name="MyFile" fileName="${LOG_HOME}/${FILE_NAME}.log"
                                 filePattern="${LOG_HOME}/${date:yyyy-MM}/${FILE_NAME}%d{yyyyMMddHHmm}-%i.log"
                                 >
            <!--显示的格式-->
            <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"/>
            <Policies>
                <OnStartupTriggeringPolicy minSize="1"/>
                <!--文件大小超过多少生成新文件-->
                <SizeBasedTriggeringPolicy size="10 MB"/>
                <!--多长时间生成一个新的LOG(按分钟)-->
                <TimeBasedTriggeringPolicy interval="120"/>
            </Policies>
            <!--默认储存多少个文件-->
            <DefaultRolloverStrategy max="10"/>
        </RollingRandomAccessFile>
    </Appenders>
    <Loggers>
        <!--异步日志 additivity防止重复日志-->
        <AsyncLogger name="com.liming.tool" level="trace" additivity="false" includeLocation="true">
            <AppenderRef ref="MyFile"/>
            <AppenderRef ref="Console"/>
        </AsyncLogger>
        <!--局部日志-->
        <!--        <Logger name="com.liming.tool" level="all" additivity="false">-->
        <!--            <AppenderRef ref="MyFile" />-->
        <!--            <AppenderRef ref="Console"/>-->
        <!--        </Logger>-->
        <!--全局日志 level日志级别-->
        <Root level="error">
            <AppenderRef ref="MyFile"/>
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>